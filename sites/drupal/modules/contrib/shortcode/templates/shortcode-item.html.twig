{#
/**
 * @file
 * Default theme implementation for the item shortcode.
 *
 * Available variables:
 * - type: The element type (div or span)
 * - attributes: Attributes array.
 * - text: The content.
 */
#}
<{{ type }}{{ attributes }}>{{ text | raw  }}</{{ type }}>